<h1>タスク一覧</h1>
  <ul>
    <% @tasks.each do |task| %>
    <li>
      <%= link_to task.id, task %>
      <%= link_to task.content, task %>
      <%= link_to task.status, task %>
    </li>
    <% end %>
  </ul>

<%= link_to "新しいタスクを登録する", new_task_path %>
